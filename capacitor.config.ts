import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'scroll-assist-bug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
